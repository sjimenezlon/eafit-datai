import ResumenClient from './ResumenClient';

export const metadata = {
  title: 'Resumen integral | Eafit - DatAI',
  description:
    'Lección resumen de Sistemas de Gestión de Bases de Datos: SQL, modelo entidad-relación, normalización, índices, ACID, modelos de datos, RAG, plataformas, gestión de proyectos y tendencias.',
};

export default function ResumenPage() {
  return <ResumenClient />;
}
